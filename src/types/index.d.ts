export interface MemoryRecord {
  id: number;
  code: string;
  content: string;
  creationDate: string;
}

export interface EquipmentRecord {
  imageUrl: string;
  parseString: string;
}

export interface CharacterRecord {
  imageUrl: string;
  name: string;
}
